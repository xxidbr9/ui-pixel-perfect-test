
import ExportBase from '../../components/export-base/export_base';

const ExportTemplate = () => {
  return (
    <ExportBase isButtonDisabled={false} isDropdownOpen={false} isExported={false} />
  )
}

export default ExportTemplate